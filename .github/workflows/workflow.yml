name: test-workflow1

on:
  push:

jobs:
  sample-job:
    runs-on: ubuntu-latest
  
    steps:
      - name: fail
        uses: browser-actions/setup-firefox@latest
        with:
          firefox-version: 91.6.0-esr
      - run: firefox --version
